#include<stdio.h>
#include <stdlib.h>
#include "EstructProtot.h"

int main(){
	int i, n=0; //El malloc trabaja con punteros
	char c;
	FILE *pf;
	
	AnalisisDatos *lista;
	
	//Abrimos el archivo
	pf = fopen("dataset (2).csv", "r");
	
	if (pf == NULL){
		printf("Error al abrir el archivo\n");

	return -1;
	}

	
	//contamos el n de filas
	while ((c = fgetc(pf)) != EOF) {
		if(c=='\n'){
			n++;
		}
		
	}
	
	/*COMPROBACIÓN
	printf("Este es el numero de FILAS: %i\n",n);
	printf("Este es el caracter c: %c\n",c);*/
	rewind(pf);
	
	// Aqui declaramos el fichero 
	lista = (AnalisisDatos *)malloc(n * sizeof(AnalisisDatos));
	
	for(i=0;i<n;i++){
	//fscanf (datosin,"%s",filas[i].filasenteras); //no las coje bien porque no todas las filas tienen el mismo tamaño
		if(fgets(lista[i].linea,sizeof(lista[i].linea),pf)){//fgets COJE SALTOS DE LINEA. fgets(cadenaleida, numcaracteres, nombredelfichero)
															
			lista[i].linea[strlen(lista[i].linea)-1]='\0'; // Cambiamos \n por \0
	
		}
	}
	/*COMPROBACIÓN DE LA LECTURA DEL FICHERO
	for(i=0;i<n;i++){
		printf ("%s\n",lista[i].linea);
	
	}*/
	
	fclose(pf); //CERRAMOS FICHERO DE LECTURA
	
	//IMPRIMIMOS EN EL ARCHIVO. 
	
	/*fprintf(pf,"%s ",lista[0].linea);*/ //PRIMERA FILA DE TITULO
	
	fclose(pf);
	
	/*reservar_actividad(lista, n);*/
	free(lista);
 	system ("pause");   
 	
return 0;	
}


